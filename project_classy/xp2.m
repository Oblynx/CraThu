clear;
import simulation.*;
import bots.*;
runtimes=1;
%% World {N, T, Bounds, trailnum, (palette), v0, CaptureRadius, WaypointCap, framerate
wpars={...
	2,...
	300,...
	100,...
	5,...
	3,...
	1,...
	0,...
	25};
w=World(wpars);
%% Bots {x (pos), v (vel), c, strategy,  WP, wpc}
bpars1={...
	[90, 90, 80],...
	[0, -1, 0],...
	0.9,...
	[],...
	0,...
	0};
bpars2={...
	[-80, -95, 90],...
	-[0, -1, 0],...
	0.3,...
	[],...
	0,...
	0};
bots{1}=Evader1( 0, bpars1 );
bots{2}=Pursuer1( 1, bpars2 );
RunSimulation
