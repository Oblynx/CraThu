%%% Sample Experiment %%%
% Experiments are minimalistic
clear;
import simulation.*;
runtimes=1;
%% World {N, T, Bounds, trailnum, (palette), v0, CaptureRadius, WaypointCap, framerate
wpars={...
	2,...
	300,...
	100,...
	5,...
	'grbcmykw',...
	3,...
	2,...
	5,...
	20};
w=World(wpars);
%% Bots {x (pos), v (vel), c, strategy,  WP, wpc}
bpars1={...
	1*w.Bounds* [(-1-0.5), (1-0.5)],...		% wpars(3) = w.Bounds
	3*[0.9980, -0.0628],...
	0.9,...
	'simulation.Strat.Evader1(bots, 1, world)',...
	[50 50; -50 50; -50 -50; 50 -50],...
	[1 0 0 0]};
bpars2={...
	1*w.Bounds* [(1-0.5), (-1-0.5)],...
	3*[-1, 0],...
	0.4,...
	'simulation.Strat.Pursuer1(bots, 2, world)',...
	[50 50; -50 50; -50 -50; 50 -50],...
	[1 0 0 0]};
bots=Bot(0, bpars1, bpars2);
%% Run simulation
RunSimulation
